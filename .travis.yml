language: ruby
rvm:
  - "1.9.3" 
env: TEST_DATABASE_URL=postgres://postgres@locahost/travispopup RAILS_ENV=test
before_script:
  - rake db:create db:migrate
bundler_args: --without development assets
services:
  - elasticsearch
